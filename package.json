{
  "name": "pixelplugins-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "npm run validate",
    "validate": "node scripts/validate-site.mjs",
    "preview": "vite --host 127.0.0.1",
    "sync:partials": "node scripts/sync-partials.mjs"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
